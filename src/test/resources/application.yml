camel:
  springboot:
    name: CamelRestSql
  component:
    servlet:
      mapping:
        enabled: true
        context-path: /cnad-api/*

quickstart:
  generateOrderPeriod: 1s
  processOrderPeriod: 3s
